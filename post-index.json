---
layout: nil
---
{
  "posts" : [
  {% for post in site.posts %}
    {
      "title": "{{ post.title | replace: '"', '&quot;' }}",
      "href": "{{ post.url }}"
  {% if forloop.last %}
    }
  {% else %}
    },
  {% endif %}
  {% endfor %}
  ]
}
